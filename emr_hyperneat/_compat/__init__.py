"""Vendored compatibility shim.

The minimal set of modules from the original geenns research framework that
EMR-HyperNEAT depends on (the abstract algorithm base, a config manager, the
TensorNEAT adapter/config generator, schema models, and JAX environment helpers),
copied verbatim so the algorithm runs standalone without the geenns package.

This is an internal implementation detail, not a public API.
"""
